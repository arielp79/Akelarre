[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # Sobrescribir en el dashboard de Netlify si hace falta
  VITE_API_URL = "https://akelarre-api.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
